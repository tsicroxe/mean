<script>

// $('#id').click(function (){
//     socket.emit("button_clicked", {reason: "because I want to learn about sockets!"});
// });
// socket.on('server_response', function (data){
//     console.log('The server says: ' + data.response);
// });

//play with sockets
</script>

<section ng-controller='topicController'>
  <a href='#!/'>Dashboard</a>
  <a href='#!/'>Logout</a>
  <h2>{{topic.user.first_name}} posted a topic:</h2>
  <h3>{{topic.name}}</h3>
  <p>{{topic.description}}</p>


<div class='posts' ng-repeat='post in posts'>
    <h4 style='bold'>{{post.user.first_name}}: {{post.post}}</p>

    <div ng-repeat='comment in post.comments track by $index'>
      <p>{{comment.name}}: {{comment.reply}}</p>
    </div>
    <form ng-submit='createComment(comment, post._id)'>
      <input id='reply' type='textarea' ng-model='comment.comment'/>
      <input type='submit' value='Reply' />
    </form>
  </div>

<form ng-submit='createPost(post)'>
  <label>Post your comment here: <input type='textarea' ng-model='post.post'</label>
  <input type='submit' value='Post' />
</form>
</section>
